
```javascript
__filename : //return the absolute path of the current executing file .  
__dirname  : //return the absolute path to the directory that containing the current executing file .  
process.cwd(): //return the current working directory  
process.chdir('/'); //change the currrent working directory to /

process.execPath ;// return the node executable path // /usr/local/bin/node

atime representing the file`s last access time ;
mtime representing the file`s last modification time ;
ctime representing the last time the file`s inode was changed ; 

fs.watch();//can be used to watch the specific files for modification.
```

